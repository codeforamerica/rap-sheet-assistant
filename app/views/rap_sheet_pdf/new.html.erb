<%= react_component("Uploader", {}, prerender: true) %>

<script>
  var store = {
    selectedFile: null
  };

  new Vue({
    el: '#vue-app-div',
    data: store,
    methods: {
      fileInputChanged: function (event) {
        store.selectedFile = event.target.files[0].name
      },
      clearFileInput: function () {
        store.selectedFile = null;
      }
    }

  });
</script>
